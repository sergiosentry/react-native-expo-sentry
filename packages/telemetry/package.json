{
  "name": "@phantom-labs/telemetry",
  "version": "1.0.0",
  "description": "Centralized telemetry package consumed by Phantom services for network logging.",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "check-types": "tsc --noEmit",
    "watch": "tsc -w",
    "build": "tsc",
    "lint": "eslint . --ext .js,.jsx,.ts,.tsx",
    "test": "jest --passWithNoTests"
  },
  "dependencies": {
    "@sentry/browser": "7.40.0",
    "@sentry/integrations": "7.40.0",
    "@sentry/react": "7.40.0",
    "@sentry/tracing": "7.40.0",
    "@sentry/types": "7.40.0",
    "sentry-expo": "^6.1.0"
  },
  "devDependencies": {
    "@types/jest": "^28.1.6",
    "jest": "^28.1.3",
    "ts-jest": "^28.0.7",
    "typescript": "^4.9.5"
  }
}
